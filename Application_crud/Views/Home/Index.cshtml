@model IEnumerable<Application_crud.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>List Of Employee</h2>

<p>
    @Html.ActionLink("Create New Form", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categryId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categryId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
